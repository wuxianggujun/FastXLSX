---
name: fastxlsx-test-quality
description: "添加或排查 FastXLSX CTest、OpenXML、profile 和 benchmark evidence。"
---
# FastXLSX Test Quality

先按 `docs/TESTING_WORKFLOW.md` 定级并执行最小充分验证：T0 文档/skill 只跑静态门禁；T1 小功能/窄修复只构建直接 target，运行 focused 与必要邻接用例，默认不跑 production 全量 CTest；只有 active queue 明确的大功能收口、release/milestone、构建测试基础设施大改或用户明确要求的 T2 才跑全量。Profile/package/install/Office smoke 只在改动实际触及对应边界时追加，不机械扩矩阵。

关键门禁：
- Patch preservation、side effects、failure-before-state-change、retry/reopen。
- Existing-workbook internal hyperlink 覆盖 insert/append/self-closing container、XML escaping、duplicate/range overlap、schema ordering、existing `.rels` preservation、no new relationship/content type、rename/added worksheet、unknown part、failure hook、save retry 和 reopen；external hyperlink 另测既有/缺失 `.rels`、owner-local relationship id、`TargetMode="External"`、缺失 `xmlns:r` 注入、relationship/XML 同事务、escaping、rename/reopen 和 failure-before-state-change。
- Existing-workbook data validation add 覆盖 single-/multi-range `sqref`、formula1/formula2、prompt/error escaping、insert/append/self-closing container、缺失 count 补齐、count/direct-child mismatch fail、schema ordering、existing relationship/unknown-part preservation、no new relationship/content type、rename/added worksheet、failure hook、save retry 和 reopen；remove 还覆盖当前 effective zero-based index、same-session add/remove、非最后/最后 child、count normalization、strict unsupported-source rejection、no-state-pollution、staging/save retry、rename/added worksheet与 relationship preservation。Streaming metadata test 同时验证共享 serializer 不回归。
- Existing-workbook conditional-format add/update/remove 覆盖 two-/three-color scale、basic data bar、basic `3Arrows` icon set、四种 update rule 的 single/`span`/initializer-list multi-range、existing/same-session priority continuation/preservation、planned rename/added worksheet、三个独立 diagnostics、schema-safe append/source-order replacement 与 effective-index source/update/final removal；另测 prefixed QName、invalid rule/range/index、root/QName/nesting/schema/direct-child/priority source rejection、strict advanced/custom/dxf/formula/cellIs/multiple-rule rejection、failure hook、save retry/reopen，以及 cells、relationships、content types、styles/dxf、calc metadata、unknown parts preservation。按 T1 只构建并运行 `fastxlsx.workbook_editor_public_conditional_formatting` 与必要邻接 targets；focused 未暴露全局风险时不跑 production 全量 CTest。
- Existing-workbook table lifecycle 覆盖 shared `TableOptions`/serializer；add 的 missing/existing/self-closing `tableParts`、existing/missing `.rels`、`xmlns:r`、relationship/id/part/content-type/manifest coordination；update 的 source identity/order preservation、same-session add、identical no-op、missing/name/range conflict；remove one/last、missing、name/range reuse 与 id/part session reservation。共同覆盖 source/planned rename/same-session added worksheet、effective overlay、workbook-wide ASCII-insensitive name/id/part conflict、same-sheet overlap、bounded projection、无 child relationship、percent-encoded/query/fragment URI alias inbound ownership rejection、cell/calc/unknown-part preservation、final/add/update/remove diagnostics、failure hook、save retry/reopen 和 non-sync 边界。按 T1 只构建并运行 `fastxlsx.workbook_editor_public_tables`、`fastxlsx.worksheet_table_reader`、`fastxlsx.streaming.metadata` 与静态门禁；focused 未暴露全局风险时不跑 production 全量 CTest。
- Existing-workbook auto-filter 覆盖 missing insert、self-closing/nested existing complete replace、clear existing/absent、invalid/missing ref、duplicate/nesting/schema ordering、table-local filter/relationship/content-type/unknown-part preservation、rename/added worksheet、changed + optional-range diagnostic、failure hook、save retry 和 reopen；Streaming metadata test 同时验证共享 serializer 不回归。
- Existing-workbook merged-cell 覆盖 missing/append/self-closing container、exact child/final-container removal、absent no-op、invalid/single-cell/duplicate/overlap、count/ref/direct-child/nesting/schema ordering、rename/added worksheet、addition/removal diagnostic、failure hook、save retry/reopen 和 unknown-part preservation；必须直接核对 worksheet cell value/style/formula payload、table part、workbook `calcPr`、`xl/calcChain.xml`、workbook relationship 与 content type 均保持不变，并运行 Streaming shared serializer regression。
- Existing-workbook freeze-pane 覆盖 missing/self-closing sheetViews/primary sheetView、append/replace/remove、row-only/column-only/dual-axis、zero clear/absent no-op、QName/multiple views、selection preserve/fail、split/frozenSplit/pivot/schema rejection、rename/added worksheet、final split diagnostic、failure hook、save retry/reopen 和 unknown-part preservation；必须核对 cells、table part、worksheet/workbook relationships、content types、`calcPr` 与 `calcChain` 均保持不变，并运行 Streaming shared serializer、精确 split 上界与 zero-clear regression。
- Save transaction/watermark：stage → package write → state commit、post-stage failure 保持 dirty/counts/error、retry 写最新值、成功清 unsaved、move 转移状态。
- In-memory typed strict category/context、explicit lossy、generic policy mismatch、malformed-source precedence、guardrail、no pollution、`last_edit_error()` preservation。
- C8 worksheet-local hyperlink structural sync 直接运行 public-state insertion/deletion 与独立 structural-hyperlinks target，并追加 Patch internal hyperlink 与 bounded hyperlink reader 邻接 target；覆盖四轴 shift/expand/clip/full removal、clipped single-cell、source/same-session Patch internal/external metadata、QName/attribute-order/quote/location/target/display/tooltip preservation、empty container、shared/exclusive relationship、非-hyperlink id 复用拒绝、duplicate/overlap/bounds、组合单次 rewrite/commit、空 CellStore、cell/formula/style/unknown-part/untouched worksheet preservation、PackageEditor staging-hook failure-before-state-change与 dirty/save retry/reopen。Merged/filter/validation 既有 gate 保持；下一 T1 子切片评估 conditional formatting，table/drawing/comment/VML 继续 non-sync。
- Streaming row order、无 DOM/dense matrix。
- Streaming classic-note writer 覆盖 one-based coordinate/Excel bounds、empty/duplicate/detached/closed rejection、unwritten/max target、author first-use dedup/source order、UTF-8/XML escaping/`xml:space`、simple comments、hidden VML coordinates、compact per-worksheet part numbering、content types、external hyperlink + spreadsheet drawing + VML/comments + table relationship id 与 suffix schema order、本库 reader stored/production round-trip、成功 close 后 note construction state release、失败 state 不污染，以及 OpenPyXL/Excel reopen。不得把 reopen 扩大成 rich/threaded/visibility/shape 或 existing-workbook edit claim。
- Existing-workbook classic-note Patch 覆盖 one-based bounds、empty/duplicate、missing/max target、same-session regeneration、canonical source comments/VML exact audit、update/remove missing 与 identical no-op、final/add/update/remove diagnostics、最后一条 `<legacyDrawing>`/relationship/part/content-type 清理、其他 VML part preservation、author first-use dedup、UTF-8/XML escaping/whitespace、hidden VML、compact paired numbering、relationship/content-type/suffix ordering、rich/threaded/unknown VML rejection、unknown entry/unrelated relationship/cell preservation、rename/added worksheet/remove guard、failure hook、save retry、stored/production reader round-trip 与 OpenPyXL/Excel reopen。Source takeover 还必须覆盖 comments/VML 被另一 worksheet、其他 part 或 package-root relationship 通过 percent-encoded path 或 query/fragment URI alias 引用时 fail 且不污染状态；不得扩大成任意 source comments/VML merge、rich text、shape customization 或通用 VML 编辑。
- Public bounded reader 覆盖 stored/production DEFLATE、row/cell callback order、typed scalar/shared/style/formula-cached projection、borrowed copy、callback exception unchanged + entry retry、XML window/cell-text guardrail、coordinate order、relationship presence、rich/formula metadata rejection 与 malformed diagnostics。
- Public bounded sharedStrings companion 覆盖 stored/production DEFLATE、simple/empty/entity decode、zero-based source order、borrowed copy、超过 package input chunk 的 token、callback exception unchanged + entry retry、XML window/item-text guardrail、missing/external/duplicate relationship、percent-decoded target、part/content-type audit、rich/phonetic/extension/extra metadata rejection与 malformed diagnostics。
- Public bounded sharedStrings run companion 覆盖 stored/production DEFLATE、item start/run/end source order、simple 单 run compatibility、rich boundary、borrowed text copy、owning bold/italic/direct-ARGB、start/run/end callback exception unchanged + entry retry、超过 package input chunk 的 token、XML window/item/run/runs-per-item/nesting guardrail、relationship/target/part/content-type audit，以及 mixed shape、phonetic/extension、非默认 font/theme/tint、unsupported property 与 malformed QName/boundary diagnostics。
- Public bounded cell-formats companion 覆盖 stored/production DEFLATE、custom number-format/cellXfs source order、format-code entity decode/borrowed copy、number-format 与 cell callback exception unchanged + entry retry、超过 package input chunk 的 token、XML window/format-code/nesting/custom-id count guardrail、container count/duplicate id、missing/external/duplicate relationship、percent-decoded target、part/content-type audit、standard false/no-op xf flags，以及 enabled border/base-style/protection/quote/pivot、nested/unsupported alignment 与 malformed diagnostics。
- Public bounded style-components companion 覆盖 stored/production DEFLATE、zero-based font/fill source order、owning bold/italic/direct-ARGB 与 none/gray125/solid values、font/fill callback exception unchanged + entry retry、XML window/nesting/font/fill count guardrail、container count、missing/external/duplicate relationship、percent-decoded target、part/content-type audit，以及 non-default font/theme、unsupported color/pattern/gradient 与 malformed nesting diagnostics。
- Public bounded worksheet metadata companion 覆盖 stored/production DEFLATE、primary/other sheetView audit、frozen pane/root auto-filter/zero-based merged-range source order与 owning copy、callback exception unchanged + entry retry、XML window/nesting/reference/view/merge count guardrail、duplicate/nesting/QName/schema/count/range-overlap/unsupported pane diagnostics，以及 package/relationship/content-type no-side-effect。
- Public bounded worksheet data-validation companion 覆盖 stored/production DEFLATE、zero-based source order、owning multi-range `sqref + DataValidationRule`、entity decode、formula/prompt/error projection、callback exception unchanged + entry retry、absent/empty container、XML/nesting/validation/range/sqref/formula/metadata-text guardrail、count/direct-child/QName/schema/boolean/enum/formula-shape diagnostics、foreign extension local-name disambiguation，以及 package/relationship/content-type no-side-effect。
- Public bounded worksheet hyperlink companion 覆盖 stored/production DEFLATE、zero-based source order、owning A1 range/internal location/external target/display/tooltip、internal no-`.rels`、callback exception unchanged + entry retry、absent/empty container、XML/nesting/ref/relationship-id/target/metadata-text guardrail、relationship missing/type/TargetMode/target diagnostics、namespace scope/duplicate semantic id/container/direct-child/QName/schema/unsupported metadata、duplicate/range overlap、foreign extension local-name disambiguation，以及 package/relationship/content-type no-side-effect。
- Public bounded worksheet conditional-formatting companion 覆盖 stored/production DEFLATE、zero-based source order、owning multi-range `sqref`、priority、two-/three-color scale、basic data bar、basic `3Arrows` icon set、show/reverse flags、callback exception unchanged + entry retry、absent container、XML/nesting/format/range/sqref guardrail、missing/invalid `sqref`、priority、rule shape、`cfvo`/color/icon threshold、ARGB、schema/QName/namespace diagnostics、advanced/custom/dxf/formula/cellIs rejection、foreign extension local-name disambiguation，以及 package/relationship/content-type no-side-effect。
- Public bounded worksheet table companion 覆盖 stored/production DEFLATE、`tableParts` zero-based source order、owning id/range/name/displayName/basic columns、writer-compatible totals function/label、table-local auto-filter/style flags、callback exception unchanged + worksheet retry、absent/foreign-extension container、XML/nesting/table/id/target/name/column/range guardrail、count/direct-child/QName/relationship namespace/schema diagnostics、relationship missing/type/TargetMode/percent-target/unique-part/content type audit、table root/header/totals/filter/style audit、calculated/other totals formula/full-filter/extension rejection，以及 package/relationship/content-type no-side-effect。
- Public bounded worksheet image companion 覆盖 stored/production DEFLATE、drawing zero-based source order、owning `editAs`/from-to marker/EMU offset/transform extent/name/description/format/encoded size、unique media reuse、entity decode、callback exception unchanged + worksheet retry、absent drawing、XML/nesting/image/id/target/name/description/numeric/media guardrail、worksheet/drawing QName/namespace/schema diagnostics、duplicate semantic drawing id、relationship missing/type/TargetMode/percent-target/part/content type audit、PNG/JPEG signature/CRC、`xdr:oneCellAnchor` / `xdr:absoluteAnchor` 元素、chart/shape/group/connector、crop/rotation/position transform、picture hyperlink rejection，以及 package/relationship/content-type no-side-effect。
- Public bounded worksheet comments companion 覆盖 stored/production DEFLATE、comments-part zero-based source order、owning one-based row/column/resolved author/simple text、entity decode/self-closing values、callback exception unchanged + worksheet/comments retry、absent comments 与 optional legacyDrawing、XML/nesting/comment/author/per-author/aggregate-author/text/ref/authorId/shapeId/relationship id/target guardrail、unique standard internal comments relationship/percent-target/part/content type audit、authorId bounds、duplicate ref、optional numeric shapeId、worksheet-local threaded-comment relationship rejection、persons part no-follow/no-projection、VML relationship missing/type/TargetMode/target/part/content-type/entry-presence audit、rich/phonetic/extension/unsupported metadata 与 malformed QName/schema diagnostics，以及 package/relationship/content-type no-side-effect；VML payload/visibility/shape 不解析。
- No-images disabled-feature runtime smoke 必须实际调用 image helper 与 `read_worksheet_images()` symbol 并确认抛错。

普通 CTest timeout 为 60 秒；public-state 测试已全部拆为 standalone targets，不再保留专用 120 秒 legacy shard。

Benchmark 不进默认 CTest。Release 只引用 `benchmarks/evidence/` 中通过 `validate_benchmark_evidence.py` 的 bundle；当前 4 个 production Streaming bundle、17 个 Patch bundle 与 1 个 OpenXLSX reference bundle，共 22 个，只支持 manifest 限定的单机 workload 结论。Streaming executable/matrix 使用 schema v6/v3，分列 generation/package-close/total wall 与 process CPU、body-buffer/resource-lifecycle 与 pre-warmup/warmed observation，并校验 CPU 总账；Patch `WorkbookEditor` benchmark executable/matrix 使用 schema v20/v15，保留 v6 direct-range/single-pass traffic、v7 staged CRC、v8 至 v15 fast-path/batching/prefetch/writer-call telemetry、v16 至 v18 editor CPU/CRC backend/fused CRC gate、v19 多 worksheet aggregate，并增加按 package 顺序输出的 rewritten worksheet entry 明细数组。数组 count/order、逐字段 sum/max 与 sheet1 legacy 字段必须和 aggregate 总账一致；stored backend 保持数组为空。Patch matrix 支持一次记录多个 output level，必须分列 copied source/output compressed bytes；多 worksheet profile 必须核对每表 names/value/dimension、edits/inserted、transform/CRC segments 与 OpenPyXL。Isolated package-writer executable/matrix 为 schema v2、balanced paired runner 为 schema v1，internal Patch CRC/fusion paired runner 为 schema v3。最新 tracked Streaming general matrix、Patch general matrix、Patch worksheet-scaling 与 isolated package-writer evidence 分别为 v5、v9、v20/v2、v1。Warm-up raw result 必须保留；第一轮 observation 只能标记 `cache_control=none`、`cold_cache_claim=false`，release representative 继续使用 warmed measured median。Patch paired runner 必须让两个 profile 在两个 position 等频出现，并逐轮核对 logical/compressed fingerprint、CRC 与 size；fused path 必须验证 completed-entry CRC、same-size mutation output protection 与 retry。Windows production 的至少 4 MiB staged file chunk 还必须报告固定 512 KiB input peak 与 1 MiB prefetch buffer，小/raw-copy/stored/non-Windows guard 保持同步且 prefetch traffic 为零。Planned raw-copy entry 必须比较 exact compressed payload 并核对 names/count/bytes，且 DEFLATE writer CPU 必须为 0。Minizip CPU envelope 包含 backend bookkeeping，不是纯 encoder CPU；direct-zlib engine CPU 只覆盖 `deflateInit2`/`deflate`/`deflateEnd`，CRC/read/raw-output write/entry close 必须独立。Relationship-bearing fixture 还必须验证 worksheet binding 与 `.rels` target。Reference 必须记录 API、save/compression 差异、output size 与 peak working set，任何结果不得泛化到未覆盖 workload。

Worksheet scaling runner schema v2 的 1/2/4-sheet variants 必须使用反转 rotation，让每个 variant 在每个 position 等频出现；`fixed-shape` 与 `fixed-total` 分开报告，fixed-total 明确 row-number/XML-size 并非 byte-identical。保留全部 raw result、position statistics、同轮 baseline ratio、逐 worksheet entry 跨 measured runs 的 min/median/max 与代表 OpenPyXL 状态。Windows process CPU 约 15.625 ms 量化使快速小 entry 可合法为 0，正 DEFLATE CPU gate 只对至少 4 MiB rewritten target 强制。

新的 standalone package-writer bundle 使用 `package-writer` manifest kind；旧的 Patch 合并 bundle 保持 `workbook-editor`，不追溯改写历史 evidence。

Production/stored/no-images 必须保持 `FASTXLSX_ENABLE_DIRECT_ZLIB_PROFILING=OFF`，并验证 unavailable selection 在 package write 前失败且不污染既有输出；one-pass round-trip/guard/CRC mutation 使用 `windows-nmake-release-direct-zlib-profile`。默认 install consumer 不得因 profiling engine 额外要求 FastXLSX 的 `ZLIB::ZLIB` export dependency。

Production/stored/no-images 也必须保持 `FASTXLSX_ENABLE_PORTABLE_CRC_PROFILING=OFF`。CRC backend A/B 只使用 `windows-nmake-release-patch-crc-minizip-profile` 与 `windows-nmake-release-patch-crc-portable-profile`；后者只改变 private PackageEditor CRC implementation，不改变 minizip package backend。
